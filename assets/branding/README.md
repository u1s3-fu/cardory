# 品牌资源

本目录存放 Cardory 应用的品牌原始素材，供各平台图标生成工具和 Flutter 界面使用。

- `app_icon_source.png`：1024×1024 主图标源图，用于生成 Android、iOS、macOS、Windows 等平台的应用图标。

这些资源通常在 `pubspec.yaml` 的 `flutter.assets` 中声明，并通过脚本或 Flutter 工具生成到各平台目录下。
