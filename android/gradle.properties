org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
android.enableJetifier=true
# Flutter 迁移器添加的过渡开关。AGP 9 已支持 Built-in Kotlin，但 Flutter 3.44 的
# 插件加载器仍会向未声明 KGP 的插件子项目强制应用 kotlin-android（如
# flutter_secure_storage / jni / path_provider_android），与 Built-in Kotlin 冲突。
# 待 Flutter 移除该兼容 shim 且全部插件完成迁移后，方可移除这两个开关。
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
